# 校园贴吧项目结构与功能说明

## 1. 项目概览

这是一个基于 Spring Boot MVC、Spring JDBC、MySQL 和原生 HTML/CSS/JavaScript 的校园贴吧系统。

- 后端：Spring Boot 3.5.6，Java 21 编译目标。
- 数据库：MySQL `campus_tieba`。
- 前端：`src/main/resources/static` 下的原生页面和脚本。
- 运行方式：Spring Boot 内置 Tomcat，默认端口 `8080`。
- 数据初始化：应用启动时自动创建数据库中的表，并写入演示用户、板块和帖子。

## 2. 目录结构

```text
项目根目录/
├─ pom.xml
├─ README.md
├─ 项目文件功能说明.md
├─ database/
│  ├─ schema.sql              # 手动初始化参考脚本
│  └─ seed-20-posts.sql       # 手动补充测试帖子的脚本
├─ src/main/java/com/tieba/
│  ├─ TiebaApplication.java
│  ├─ config/
│  │  ├─ DatabaseInitializer.java
│  │  └─ WebConfig.java
│  ├─ controller/
│  │  ├─ ActivityController.java
│  │  ├─ AuthController.java
│  │  ├─ BoardController.java
│  │  ├─ PostController.java
│  │  ├─ UploadController.java
│  │  └─ UserController.java
│  ├─ model/
│  │  ├─ Post.java
│  │  └─ User.java
│  ├─ repository/
│  │  ├─ BoardRepository.java
│  │  ├─ PostRepository.java
│  │  └─ UserRepository.java
│  └─ service/
│     └─ AuthService.java
└─ src/main/resources/
   ├─ application.yml
   ├─ database/
   │  ├─ schema.sql
   │  └─ seed.sql
   └─ static/
      ├─ index.html
      ├─ auth.js
      ├─ forum-main.html
      ├─ forum-main.js
      ├─ forum-ui.css
      ├─ i18n.js
      ├─ post.html
      ├─ post.js
      ├─ post.css
      ├─ profile.html
      ├─ profile.js
      ├─ profile.css
      └─ style.css
```

`target/` 是 Maven 编译产物，不属于源代码；`uploads/` 保存用户上传的图片和视频。根目录下的 `database/campus_tieba.db` 是早期 SQLite 遗留文件，当前 MySQL 版本不会读取它。

## 3. 构建与配置

### `pom.xml`

定义项目坐标、依赖和构建插件。

- `spring-boot-starter-web`：REST Controller、Spring MVC 和内置 Tomcat。
- `spring-boot-starter-jdbc`：`JdbcTemplate`、数据源和连接池。
- `mysql-connector-j`：MySQL JDBC 驱动。
- `spring-boot-starter-validation`：请求字段校验。
- `spring-boot-starter-test`：测试依赖。
- `spring-boot-maven-plugin`：`mvn spring-boot:run` 和打包运行。

### `src/main/resources/application.yml`

配置服务端口、MySQL 数据源、上传限制和上传目录。

- 默认地址：`localhost:3306/campus_tieba`。
- 默认账号：`root`。
- 默认密码：`root`，可通过 `DB_USERNAME` 和 `DB_PASSWORD` 覆盖。
- `createDatabaseIfNotExist=true`：连接时自动创建数据库。
- 上传目录：`uploads`。

## 4. 启动与数据库初始化

### `TiebaApplication.java`

Spring Boot 启动类，调用 `SpringApplication.run` 启动应用。

### `DatabaseInitializer.java`

使用 `@PostConstruct` 在应用启动阶段执行初始化：

1. 创建 `uploads` 目录。
2. 查询 MySQL 的 `information_schema`，判断 `users` 表是否存在。
3. 表不存在时执行 classpath 下的 `database/schema.sql`。
4. 帖子数少于 20 条时执行 `database/seed.sql`。

运行时真正使用的是 `src/main/resources/database/` 下的脚本；根目录 `database/` 下的脚本仅用于手动初始化或参考。

### `WebConfig.java`

把本地 `uploads/` 目录映射为 `/uploads/**`，使浏览器可以访问上传的媒体文件。

## 5. MVC 分层与后端实现

项目采用 Spring Boot MVC。View 通过 `fetch` 调用 Controller 的 REST API，Controller 调用 Service 或 Repository，Repository 使用参数化 SQL 访问 MySQL，结果再以 JSON 返回给 View。

### MVC 角色映射

| MVC 角色 | 项目目录/文件 | 主要职责 |
| --- | --- | --- |
| Model | `model/`、`repository/`、`database/` | 表示用户和帖子，读写 MySQL 数据 |
| View | `src/main/resources/static/` | 页面结构、样式、交互和数据展示 |
| Controller | `controller/` | 接收 HTTP 请求、调用后端层、返回 JSON |
| Service | `service/` | 封装登录注册等业务规则 |
| Config | `config/`、`application.yml` | 初始化数据库、配置上传资源和运行参数 |

### MVC 请求流程

```text
HTML/JavaScript View
   -> fetch('/api/...')
Controller
   -> Service（需要业务规则时）
   -> Repository
   -> MySQL
   <- JSON
View 更新页面
```

下面按 Model、Service、Controller 的实际文件继续说明后端实现。

### 5.1 模型层

#### `model/User.java`

使用 Java `record` 表示用户，包含用户 ID、账号、昵称、头像和用户类型。

#### `model/Post.java`

使用 Java `record` 表示帖子，包含帖子 ID、板块、发布者、时间、标题、正文、媒体地址以及点赞、转发、收藏和浏览量。

### 5.2 业务层

#### `service/AuthService.java`

封装登录和注册规则：

- 登录时查询用户、密码并校验普通用户/管理员身份。
- 注册时校验密码长度、空格和密码类别，检查账号重复后保存用户。
- 使用日志记录登录和注册结果，不记录密码。

### 5.3 数据访问层

#### `repository/UserRepository.java`

提供用户查询、密码查询、注册、统计数据和用户资料查询。

#### `repository/BoardRepository.java`

提供板块列表、用户关注板块、关注状态查询和关注/取消关注操作。

#### `repository/PostRepository.java`

提供帖子列表、帖子详情、用户帖子、收藏、浏览记录、回复、点赞、收藏、发布、编辑和删除。所有用户输入均通过参数化 SQL 传入。

### 5.4 控制层与接口

#### `AuthController.java`：`/api/auth`

- `POST /login`：登录。
- `POST /register`：注册。

#### `PostController.java`：`/api/posts`

- `GET /api/posts`：查询全部帖子或按板块查询。
- `GET /api/posts/{id}`：查询帖子详情。
- `GET /api/posts/{id}/replies`：查询回复，包含评论者 ID、昵称和头像。
- `GET /api/posts/{id}/status`：查询当前用户的点赞/收藏状态。
- `POST /api/posts`：发布帖子。
- `PUT /api/posts/{id}`：编辑自己的帖子。
- `DELETE /api/posts/{id}`：删除自己的帖子。
- `POST /api/posts/{id}/replies`：发表评论。
- `POST /api/posts/{id}/like`：点赞或取消点赞。
- `POST /api/posts/{id}/favorite`：收藏或取消收藏。

#### `UserController.java`：`/api/users`

- `GET /api/users/{id}`：用户资料和统计数据。
- `GET /api/users/{id}/posts`：用户发布的帖子。
- `GET /api/users/{id}/favorites`：用户收藏的帖子。
- `GET /api/users/{id}/history`：用户浏览记录。
- `GET /api/users/{id}/stats`：用户统计数据。

#### `BoardController.java`：`/api/boards`

提供板块列表、用户已关注板块、板块关注状态和关注操作。

#### `ActivityController.java`

提供收藏和浏览记录写入接口：`/api/favorites`、`/api/history`。

#### `UploadController.java`：`/api/upload`

接收图片或视频文件，生成 UUID 文件名并返回 `/uploads/...` 访问地址。

## 6. 前端页面与脚本

### 登录注册

#### `static/index.html`

登录和注册页面，包含普通用户/管理员模式切换。

#### `static/auth.js`

提交登录注册请求；登录成功后把用户信息保存到 `sessionStorage`，跳转论坛主页。

### 论坛主页

#### `static/forum-main.html`

提供导航、用户信息、统计数据、板块筛选、帖子流、发布入口、搜索入口和设置入口。

#### `static/forum-main.js`

负责加载帖子、板块和用户统计，渲染帖子卡片，处理点赞、收藏、分享、发布、编辑、删除、搜索和视图切换。帖子作者头像可进入个人主页。

#### `static/style.css`

登录页和论坛主页的整体布局、颜色、字体、头像、帖子卡片和响应式样式。

#### `static/forum-ui.css`

发布、设置、搜索等弹窗的遮罩、面板和控件样式。

#### `static/i18n.js`

提供中英文界面文本和语言切换。

### 帖子详情

#### `static/post.html`

显示完整帖子、发布者头像、板块信息、媒体、互动按钮、全部回复和评论表单。

#### `static/post.js`

加载帖子和回复，处理点赞、收藏、回复、编辑、删除和板块关注。发布者头像、评论者头像和评论者昵称均可跳转到个人主页。

#### `static/post.css`

详情页、回复列表、评论者头像、返回按钮和媒体区域样式。

### 用户主页

#### `static/profile.html`

显示用户资料、账号、统计数据和其发布的帖子。

#### `static/profile.js`

调用用户资料和用户帖子接口，渲染个人主页，并支持返回帖子详情或论坛主页。

#### `static/profile.css`

个人主页的用户信息、统计区域和帖子列表样式。

## 7. 数据库结构

`src/main/resources/database/schema.sql` 创建以下表：

- `users`：用户和账号信息。
- `boards`：论坛板块。
- `posts`：帖子正文、媒体和互动计数。
- `user_follows`：用户关注的板块。
- `user_likes`：用户点赞关系。
- `user_favorites`：用户收藏关系。
- `browsing_history`：用户浏览记录。
- `replies`：帖子回复和评论者关系。

`seed.sql` 提供演示账号、四个板块和 20 条测试帖子。初始化逻辑使用 `INSERT IGNORE`，重复启动不会重复插入相同演示数据。

## 8. 典型用户流程

1. 用户访问 `/`，完成登录或注册。
2. 登录成功后进入 `/forum-main.html`。
3. 主页加载板块、统计数据和帖子列表。
4. 点击帖子进入 `/post.html?id=...`。
5. 点击帖子发布者或评论者头像进入 `/profile.html?id=...`。
6. 在详情页可以点赞、收藏、关注板块、发表评论或管理自己的帖子。
7. 上传的图片和视频由 `/api/upload` 保存到 `uploads/` 并通过 `/uploads/**` 访问。

## 9. 运行命令

```powershell
# 按需设置 MySQL 账号
$env:DB_USERNAME = "root"
$env:DB_PASSWORD = "root"

# 启动项目
mvn spring-boot:run
```

浏览器访问 `http://localhost:8080`。停止程序时，在运行 Maven 的终端按 `Ctrl + C`。
