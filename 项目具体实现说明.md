# 校园贴吧项目具体实现说明

本文档记录项目各部分的实际实现，不只描述目录结构，还说明主要类、方法、接口、数据流和运行行为。

## 1. 技术实现总览

项目使用 Spring Boot MVC：

```text
浏览器 View
  -> fetch 请求
Controller
  -> Service（需要业务规则时）
  -> Repository
  -> MySQL
  <- JSON 响应
View 更新页面
```

- Spring Boot：3.5.6。
- Java：项目编译目标为 Java 21，当前环境可使用更高版本 JDK 运行。
- 数据库：MySQL `campus_tieba`。
- 数据访问：Spring JDBC `JdbcTemplate`。
- 前端：原生 HTML、CSS 和 JavaScript。
- 静态资源：Spring Boot 从 `src/main/resources/static` 提供。
- 默认端口：`8080`。

## 2. 启动和配置实现

### 2.1 `pom.xml`

`pom.xml` 负责依赖和构建：

- `spring-boot-starter-web`：MVC、REST 和内置 Tomcat。
- `spring-boot-starter-jdbc`：`JdbcTemplate` 和连接池。
- `mysql-connector-j`：MySQL 驱动。
- `spring-boot-starter-validation`：`@Valid`、`@NotBlank` 等校验注解。
- `spring-boot-starter-test`：测试依赖。
- `spring-boot-maven-plugin`：启动和打包插件。

`java.version` 设置为 `21`，避免使用 Java 25 字节码时与 Spring Boot 插件产生 major version 69 冲突。

### 2.2 `application.yml`

主要配置如下：

- `server.port`：设置 HTTP 端口 `8080`。
- `spring.datasource.url`：连接 `localhost:3306/campus_tieba`。
- `createDatabaseIfNotExist=true`：数据库不存在时由 MySQL 驱动尝试创建。
- `DB_USERNAME`：数据库用户名，默认 `root`。
- `DB_PASSWORD`：数据库密码，默认 `root`。
- `spring.servlet.multipart`：限制上传文件大小，单文件 20 MB、请求 25 MB。
- `tieba.upload-dir`：上传目录，默认 `uploads`。
- `spring.sql.init.mode=never`：关闭 Spring 默认 SQL 初始化，改由项目自己的初始化器控制顺序。

### 2.3 `TiebaApplication.java`

`main` 方法调用 `SpringApplication.run`：

1. 创建 Spring 容器。
2. 扫描 `com.tieba` 下的组件。
3. 注册 Controller、Service、Repository 和 Config。
4. 启动内置 Tomcat。
5. 执行带有 `@PostConstruct` 的数据库初始化逻辑。

### 2.4 `DatabaseInitializer.java`

该类是启动期数据库初始化入口：

- `init()`：创建 `uploads` 目录，检查表结构并写入演示数据。
- `tableExists(name)`：查询 `information_schema.tables` 判断当前数据库是否存在指定表。
- `runScript(path)`：读取 classpath SQL 文件，按分号拆分后逐条执行。
- `postCount()`：统计 `posts` 表中的帖子数量。

启动逻辑：

```text
连接 MySQL
  -> users 表不存在？
     -> 执行 database/schema.sql
  -> posts 数量少于 20？
     -> 执行 database/seed.sql
  -> Spring Boot 完成启动
```

### 2.5 `WebConfig.java`

把本地上传目录映射为 `/uploads/**`：

- 浏览器访问 `/uploads/文件名`。
- Spring MVC 将请求映射到本地 `uploads` 目录。
- 路径经过绝对路径和规范化处理。

## 3. Model 层具体实现

### 3.1 `model/User.java`

使用 Java `record` 定义不可变用户对象：

- `id`：用户 ID。
- `account`：登录账号。
- `username`：显示昵称。
- `avatar`：头像信息或头像地址。
- `userType`：`USER` 或 `ADMIN`。

### 3.2 `model/Post.java`

使用 Java `record` 定义帖子对象：

- `id`：帖子 ID。
- `boardName`：板块名称。
- `userId`、`username`、`avatar`：发布者信息。
- `createdAt`：发布时间。
- `likes`、`shares`、`favorites`、`views`：互动计数。
- `title`、`content`：帖子标题和正文。
- `imageUrl`、`videoUrl`：媒体地址。

## 4. Service 层具体实现

### `AuthService.java`

认证业务由该类统一处理。

#### `login(account, password, admin)`

1. 通过 `UserRepository.findByAccount` 查询用户。
2. 通过 `UserRepository.password` 查询密码。
3. 检查账号是否存在、密码是否匹配。
4. 根据 `admin` 标志检查管理员/普通用户身份。
5. 成功时记录审计日志并返回 `User`。
6. 失败时记录警告日志并抛出统一错误消息。

密码不会写入日志。

#### `register(account, password, username)`

1. 判断密码是否包含小写字母。
2. 判断是否包含大写字母。
3. 判断是否包含数字。
4. 判断是否包含特殊字符。
5. 要求密码长度 8 到 32 位且不能有空格。
6. 要求至少满足两类字符。
7. 查询账号是否已存在。
8. 通过 Repository 写入普通用户。
9. 记录注册成功日志。

## 5. Repository 层具体实现

所有 Repository 使用 `JdbcTemplate` 和 `?` 占位符，避免拼接用户输入。

### 5.1 `UserRepository.java`

- `findByAccount(account)`：按账号查询用户基本资料。
- `findById(id)`：按 ID 查询个人资料。
- `password(account)`：只查询登录校验所需的密码字段。
- `insert(account, password, username)`：插入普通用户。
- `stats(userId)`：统计用户发帖数、获赞数、收藏数和关注数。

### 5.2 `BoardRepository.java`

- `findAll()`：按热度倒序返回所有板块。
- `followedByUser(userId)`：查询用户已关注板块。
- `isFollowing(userId, boardName)`：检查关注状态。
- `toggleFollow(userId, boardName, on)`：插入或删除板块关注关系。
- 关注插入使用 MySQL `INSERT IGNORE`，避免重复主键报错。

### 5.3 `PostRepository.java`

#### 查询方法

- `findAll(board)`：查询全部帖子或指定板块帖子，按发布时间倒序。
- `findById(id)`：查询单条帖子详情。
- `findByUserId(userId)`：查询某用户发布的帖子。
- `findFavoritesByUserId(userId)`：联表查询用户收藏的帖子。
- `findHistoryByUserId(userId)`：联表查询用户浏览过的帖子。
- `replies(postId)`：联表查询回复、评论者 ID、昵称和头像。

#### 互动方法

- `toggleLike(userId, postId)`：检查点赞关系，插入/删除关系并同步更新帖子点赞数。
- `toggleFavorite(userId, postId, on)`：插入/删除收藏关系并同步收藏计数。
- `history(userId, postId)`：插入浏览记录并增加浏览量。
- `reply(postId, userId, content)`：插入回复。

#### 帖子管理方法

- `insert(...)`：插入帖子，空媒体地址转为空字符串，并使用 `LAST_INSERT_ID()` 获取新 ID。
- `update(...)`：只允许帖子作者编辑自己的帖子。
- `delete(...)`：先删除相关点赞、收藏、浏览记录和回复，再删除帖子。
- `isLiked(...)`：查询用户是否点赞。
- `isFavorited(...)`：查询用户是否收藏。
- `map(resultSet, row)`：把 JDBC 查询行转换为 `Post` record。

## 6. Controller 层具体实现

### 6.1 `AuthController.java`：`/api/auth`

#### `POST /api/auth/login`

请求字段：`account`、`password`、`admin`。

- 使用 `@Valid` 和 `@NotBlank` 校验账号密码。
- 成功返回 `{ "user": {...} }`。
- 业务校验失败返回 HTTP 400 和 `message`。

#### `POST /api/auth/register`

请求字段：`account`、`password`、`username`。

- 校验必填字段。
- 调用 `AuthService.register`。
- 成功返回注册成功消息。

### 6.2 `PostController.java`：`/api/posts`

- `GET /api/posts`：全部帖子或按 `board` 查询。
- `GET /api/posts/{id}`：帖子详情。
- `GET /api/posts/{id}/replies`：回复列表。
- `GET /api/posts/{id}/status?userId=...`：点赞和收藏状态。
- `POST /api/posts`：发布帖子。
- `PUT /api/posts/{id}`：编辑帖子。
- `DELETE /api/posts/{id}?userId=...`：删除帖子。
- `POST /api/posts/{id}/replies`：发布回复。
- `POST /api/posts/{id}/like`：点赞或取消点赞。
- `POST /api/posts/{id}/favorite`：收藏或取消收藏。

控制器只负责接收请求和组织响应，具体 SQL 由 Repository 执行。

### 6.3 `UserController.java`：`/api/users`

- `GET /api/users/{id}`：返回 `{ user, stats }`。
- `GET /api/users/{id}/posts`：个人主页帖子。
- `GET /api/users/{id}/favorites`：个人收藏。
- `GET /api/users/{id}/history`：浏览记录。
- `GET /api/users/{id}/stats`：统计数据。

该接口支持从帖子发布者或评论者头像进入个人主页。

### 6.4 `BoardController.java`：`/api/boards`

- `GET /api/boards`：板块列表。
- `GET /api/boards/followed?userId=...`：用户关注板块。
- `GET /api/boards/status?...`：关注状态。
- `POST /api/boards/follow`：关注或取消关注。

### 6.5 `ActivityController.java`

- `POST /api/favorites`：写入或删除收藏关系。
- `POST /api/history`：写入浏览记录。

### 6.6 `UploadController.java`：`/api/upload`

1. 接收 `MultipartFile`。
2. 拒绝空文件。
3. 读取扩展名。
4. 使用 UUID 生成唯一文件名。
5. 创建上传目录并保存文件。
6. 根据扩展名返回 `image` 或 `video` 类型及访问 URL。

## 7. View 层具体实现

### 7.1 登录注册视图

#### `index.html`

提供登录和注册表单，以及普通用户/管理员模式切换。

#### `auth.js`

- 监听登录表单提交。
- 调用 `/api/auth/login`。
- 成功后写入 `sessionStorage.tiebaUser`。
- 跳转 `/forum-main.html`。
- 注册成功后切回登录表单。
- 将后端错误显示在页面上。

### 7.2 论坛主页

#### `forum-main.html`

包含顶部导航、个人信息侧栏、帖子流、热门板块、发布按钮、搜索入口和弹窗容器。

#### `forum-main.js`

- `loadUserStats()`：加载当前用户统计。
- `loadTrending()`：加载热门板块。
- `fetchPosts()`：按当前视图、板块、收藏或历史记录请求帖子。
- `renderCard(post)`：生成帖子卡片，显示作者、媒体和互动数据。
- `renderFeed()`：刷新帖子 DOM，并绑定点击事件。
- `loadPostStatus(posts)`：批量查询当前用户的点赞和收藏状态。
- `loadFeed()`：加载数据、初始化状态并渲染列表。
- `handleAction(button)`：处理点赞、收藏、分享、编辑和删除。
- `showModal()`：生成发布、设置和搜索弹窗。
- 搜索逻辑：按标题、正文、作者和板块计算匹配度并排序。
- 作者头像：跳转 `/profile.html?id=用户ID`。

### 7.3 帖子详情页

#### `post.html`

包含返回按钮、帖子详情、发布者信息、媒体区域、互动按钮、回复列表和回复表单。

#### `post.js`

- `recordHistory()`：进入详情后记录浏览历史。
- `loadStatus()`：加载当前用户的点赞/收藏状态。
- `loadBoardStatus()`：加载板块关注状态。
- `renderPost(post)`：渲染完整帖子和互动操作。
- `loadPost()`：调用帖子详情接口。
- `loadReplies()`：加载回复并渲染评论者头像和昵称。
- 回复提交成功后重新加载回复和帖子。
- 发布者和评论者链接均跳转到 `/profile.html?id=...`。

#### `post.css`

- `.post-page`：详情页容器。
- `.detail-panel`：帖子内容面板。
- `.reply-panel`：回复面板。
- `.reply`：单条回复。
- `.reply-avatar`：固定 30 像素圆形评论头像。
- `.profile-author`：可点击的作者/评论者链接。

### 7.4 用户主页

#### `profile.html`

显示用户头像、昵称、账号、统计信息和发布的帖子。

#### `profile.js`

1. 从 URL 读取 `id`。
2. 并行请求用户资料和用户帖子。
3. 渲染用户统计数据。
4. 渲染用户发布的帖子。
5. 点击帖子返回对应详情页。

#### `profile.css`

负责个人主页卡片、统计区域、帖子列表和移动端布局。

### 7.5 公共前端文件

- `style.css`：全局颜色、字体、头像、布局、帖子卡片和响应式规则。
- `forum-ui.css`：弹窗、遮罩层和设置面板。
- `i18n.js`：中英文文字映射和界面语言切换。

## 8. 数据库具体实现

### 8.1 表关系

```text
users 1 --- n posts
users 1 --- n replies
users n --- n boards       （user_follows）
users n --- n posts        （user_likes）
users n --- n posts        （user_favorites）
users 1 --- n browsing_history
posts 1 --- n replies
boards 1 --- n posts       （posts.board_name -> boards.name）
```

### 8.2 表职责

- `users`：账号、密码、昵称、头像和用户类型。
- `boards`：板块名称、图标、创建者和热度。
- `posts`：帖子文本、媒体地址、互动计数和发布时间。
- `replies`：回复正文、回复者和所属帖子。
- `user_follows`：用户与板块的关注关系。
- `user_likes`：用户与帖子的点赞关系。
- `user_favorites`：用户与帖子的收藏关系。
- `browsing_history`：用户浏览过的帖子。

### 8.3 初始化脚本

- `src/main/resources/database/schema.sql`：运行时建表脚本。
- `src/main/resources/database/seed.sql`：运行时演示数据脚本。
- `database/schema.sql`：手动初始化参考脚本。
- `database/seed-20-posts.sql`：手动补充帖子参考脚本。
- `database/campus_tieba.db`：早期 SQLite 遗留文件，当前 MySQL 程序不读取。

## 9. 完整业务流程

### 登录流程

```text
index.html
  -> auth.js POST /api/auth/login
  -> AuthController
  -> AuthService
  -> UserRepository
  -> MySQL
  -> 返回 User
  -> sessionStorage
  -> forum-main.html
```

### 查看帖子流程

```text
forum-main.js GET /api/posts
  -> PostController
  -> PostRepository.findAll
  -> 点击帖子
  -> post.html?id=...
  -> GET /api/posts/{id}
  -> GET /api/posts/{id}/replies
  -> 渲染帖子、发布者、评论者和互动按钮
```

### 进入个人主页流程

```text
点击发布者/评论者头像或昵称
  -> /profile.html?id=用户ID
  -> GET /api/users/{id}
  -> GET /api/users/{id}/posts
  -> 渲染用户信息和帖子列表
```

### 发布帖子流程

```text
发布弹窗
  -> 可选 POST /api/upload
  -> POST /api/posts
  -> PostController
  -> PostRepository.insert
  -> MySQL
  -> 刷新论坛列表
```

## 10. 运行说明

```powershell
$env:DB_USERNAME = "root"
$env:DB_PASSWORD = "root"
mvn spring-boot:run
```

访问 `http://localhost:8080`。停止程序时在 Maven 终端按 `Ctrl + C`。
